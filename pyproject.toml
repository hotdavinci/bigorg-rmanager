[project]
name = "reels-automation-manager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115", "uvicorn[standard]>=0.30", "sqlalchemy>=2.0", "alembic>=1.13", "pydantic-settings>=2.4", "cryptography>=43", "httpx>=0.27", "python-multipart>=0.0.12", "aiofiles>=24.1", "itsdangerous>=2.2"]

[tool.setuptools]
packages = ["app"]
